<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.fdroid.fdroid.privileged"
    android:versionCode="2120"
    android:versionName="0.2.12">

    <uses-sdk
        android:minSdkVersion="31"
        android:targetSdkVersion="32" />

    <permission android:name="org.fdroid.fdroid.privileged.BROADCAST_SENDER_PERMISSION" android:protectionLevel="system|signature" />

    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <uses-permission android:name="org.fdroid.fdroid.privileged.BROADCAST_SENDER_PERMISSION" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:ignore="GoogleAppIndexingWarning">

        <service
            android:name=".PrivilegedService"
            android:enabled="true"
            android:exported="true"
            android:process=":fdroid_privileged"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="org.fdroid.fdroid.privileged.IPrivilegedService" />
            </intent-filter>
        </service>

    </application>

</manifest>
